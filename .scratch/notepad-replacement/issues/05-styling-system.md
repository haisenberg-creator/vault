Status: resolved
Type: decision

## Decision

Design system established using `/pro-max-design-system`: **Animated Rosé Pine Moon Soho Theme** with Vanilla CSS tokens, motion physics, `@keyframes roseGlowPulse`, live progress meters, and `Pixelify Sans` + `JetBrains Mono` typography.
