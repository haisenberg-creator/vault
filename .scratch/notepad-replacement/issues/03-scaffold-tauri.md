Status: resolved
Type: task
Blocked by: 01, 02

## Question

Initialize the Tauri + React + Vite project in the repository.

1. Run `create-tauri-app` or equivalent to set up the boilerplate.
2. Configure basic tooling (TypeScript, linter).
3. Ensure the app builds and runs locally.

## Answer

Scaffolded the project using Tauri v2 (`create-tauri-app`) with React + TypeScript and Vite. Installed `lexical` and `@lexical/react` dependencies. Verified `npm run build` passes without errors.
