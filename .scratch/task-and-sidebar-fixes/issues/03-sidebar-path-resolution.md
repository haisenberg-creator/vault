# 03 — Tasks Dashboard Path Resolution

**What to build:** Clicking a note in the Tasks tab correctly loads the existing note by resolving its absolute path, preventing the editor from loading a blank/new note. The UI still cleanly displays the relative path.

**Blocked by:** None — can start immediately

**Status:** completed

- [x] Clicking a note in the Tasks Dashboard tab loads the full content of the existing note.
- [x] The editor does not display a "New Note" state when navigating via the Tasks tab.
- [x] The app's UI continues to display clean, relative paths (e.g., `Projects/Eucerin.md`) rather than messy absolute system paths.
