# 05 — Graphify AI Agent Skill & Repository Indexing Script

**What to build:** Scaffolds the `.agents/skills/graphify/SKILL.md` skill and provides repository indexing scripts in `scripts/` to give AI coding assistants AST knowledge graph intelligence over the Vault codebase (ahead of the v2.0.0 in-app Graph View).

**Blocked by:** None — can start immediately

**Status:** done

- [x] `.agents/skills/graphify/SKILL.md` is created with YAML frontmatter and comprehensive instructions for AI agents on indexing and querying the codebase AST graph.
- [x] `scripts/graphify.js` (or npm runner script in `package.json`) is added to run codebase graph generation and dependency mapping locally.
- [x] Documentation explains how to generate the knowledge graph, query module relationships, and prepare for the v2.0.0 in-app Graph View.
- [x] Verified that the skill passes formatting rules and is discoverable by AI assistants.
