# 01 — TitleBar & Action Bar Polish

**What to build:** Polish the top titlebar chrome and the editor action bar. Remove the redundant `Open` button from the task action bar (an unchecked box is naturally open). Remove the file path string from the `TitleBar` so only the clean app title remains, and move the `ARCADE / WORKING` mode toggle directly beside the `Vault` app header.

**Blocked by:** None — can start immediately

**Status:** closed

- [x] The `Open` button is removed from the task action bar; `In Progress`, `Blocked`, and `Done` remain functional
- [x] The file path text is removed from the TitleBar, leaving the TitleBar clean and uncluttered
- [x] The `ARCADE / WORKING` mode toggle button is positioned directly next to the `Vault` title in the TitleBar
- [x] The right-click context menu on the Note lectern icon remains functional
- [x] Toggling between `ARCADE` and `WORKING` mode continues to work cleanly
- [x] All existing layout and action bar tests are updated and passing
